# Word Hunt Main Backend Components

## wordhuntcontroller

- Handling incoming HTTP requests
- Validating request parameters
- Calling appropriate services
- Returning API responses
- Managing API-level errors

### wordhuntcontroller Methods

| Method              | Description                                                                                                                      |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `findPOSEnglish()`  | Retrieves the given storyId from req.query and calls service layer with the storyId. Returns Tokenized Passage English Version   |
| `findPOSSanskrit()` | Retrieves the given storyId from req.query and calls service layer with the storyId. Returns Tokenized Passage Sanskrit Version. |

---

## findNounsVerbsAndAdjectivesGameEnglish && findNounsVerbsAndAdjectivesGameSanskrit Service

The core business logic layer responsible for retrieving the raw data from tokenized story:

- Processing story information
- Managing story transformation workflows

### findNounsVerbsAndAdjectivesGameEnglish && findNounsVerbsAndAdjectivesGameSanskrit Methods

| Method                        | Description                                                                                                |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `findNounVerbAndAdjEnglish()` | processes the retrieved story extracts required data and sends back response to findPOSEnglish endpoint.   |
| `getStoryById()`              | given a storyId this endpoint retreives the story from tokenized_stories collection.                       |
| `extractRequiredData()`       | helper function. Given raw data this funcction extracts only the required data for english version passage |
| `breakPassageToArray()`       | helper function. Given english passage in string format breaks it into array.                              |
| `cleanTokenizedArray()`       | helper function. Given an tokenized array removes all empty POS tokens                                     |

---

## gamecontroller

Responsible for adding story information and Game Meta generated by Noun, Verb and Adjective Game:

- Handling incoming HTTP requests
- Validating request parameters
- Calling appropriate services
- Returning API responses
- Managing API-level errors

### gamecontroller Methods

| Method              | Description                                                                                                                                                        |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `addStoryInfo()`    | Given gameId, storyId, number of nouns, verbs, adjectives and hints. Initializes the Story Info class to store to repository returns a success or failure response |
| `addGameData()`     | Given Game meta initializes the game data and returns a success or failure response.                                                                               |
| `fetchPlayerData()` | Given gameId, storyId and playerName retreives the player information from regarding the particular game played.                                                   |

---

### gameservice

Responsible for enriching token data with dictionary information:

- Retrieving definitions
- Adding word relationships
- Connecting gamerepository with controller, initializes business logic

#### gameservice Methods

| Method                   | Description                                                                                                                                                                                            |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `initWordHuntRepo()`     | Service layer call to initialize repository and add story set Ids. consumed by storySets/controller/storySetsController.js                                                                             |
| `retreiveAllMetaData()`  | Service layer call to retrieve all game data for leaderboard services.                                                                                                                                 |
| `insertsStoryInfo()`     | Service layer call to insert the passage information like number of words to be found, hints available for that particular passage.                                                                    |
| `insertGameData()`       | Service layer call to add game meta data like number of words found, time and number of hints used.                                                                                                    |
| `getPlayerInfoByStory()` | Service layer call that fetches the player game information cleans it, from all information checks if the player have completed the game and fetches that game meta data and return a custom response. |

---

## wordhuntrepo

The database access layer responsible for:

- Communicating with MongoDB
- Executing database operations
- Persisting and players game information

### gamerepository Methods

| Method                        | Description                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `initializeGame()`            | Connects with mongodb to initialize the repository as an array of objects. Initialized only once, when new story sets are added the array is updated                                                                                                                                                                                                          |
| `getAllGameInfo()`            | Connects to mongodb and Retrieves all game meta data stored from the database for leaderboard purposes.                                                                                                                                                                                                                                                       |
| `initializeStoryInfo()`       | Receives the input on the story information and verifies if the information is already registered and also verifies if the registered information is same. If the information is same the application just returns already initialized. **_ELSE THIS IS A CHECK POINT TO ADD THE STORY INFORMATION FOR ENGLISH STORIES IN FUTURE FOLLOW THE SAME PROCEDURE_** |
| `registerGameData()`          | Receives game meta data number of words found, hints used, score earned and coins earned, and persists it according by player and story played.                                                                                                                                                                                                               |
| `retrievePlayerInfoByStory()` | Given gameId ans storyId the fuction fetches the information of the player on the specific story of the active passage.                                                                                                                                                                                                                                       |
