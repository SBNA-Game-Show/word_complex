services:
  mongodb:
    image: mongo:7
    container_name: mongodb

    ports:
      - "27017:27017"

    volumes:
      - mongo_data:/data/db

    restart: unless-stopped

    networks:
      - word-complex

volumes:
  mongo_data:

networks:
  word-complex:
    driver: bridge
